import {Dimensions} from 'react-native';

export const screenHeight = Dimensions.get('window').height;
export const defaultSafeTopOffset = 50;
export const defaultOpenDuration = 450;
export const defaultCloseDuration = 300;
export const defaultInitialHeight = 420;
export const defaultBackdropColor = '#000';
export const defaultBackdropOpacity = 0.5;
export const defaultSnapPoints = [400];
export const defaultInitialIndex = 0;
export const defaultGestureMode = 'handle';
